# setup

Creates virtual virtual environemnt for python project:
```python3 -m venv venv````

Uses created venv:
```source ./venv/bin/activate```

Installs dependencies into the venv:
```pip install -r requirements.txt```

```
python3 main.py
```